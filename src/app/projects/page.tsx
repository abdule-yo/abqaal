import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Projects',
  description: 'Discover our impactful agricultural projects across Somaliland and the Horn of Africa — from greenhouse installations to farmer training programs.',
}

export default function ProjectsPage() {
  return (
    <main>
      {/* Sections will be built one by one */}
    </main>
  )
}
